AnimationChannelInterpolationType
=================================

Declaration
-----------

.. code-block:: cpp

	enum class AnimationChannelInterpolationType {
		Step,
		Linear,
		CubicSpline,
		Unknown
	};

Notes
-----

The :doc:`/types/AnimationChannel/index`'s interpolation type. It represents the way the values will get interpolated between two keyframes.